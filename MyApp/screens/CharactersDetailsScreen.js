import React, {useState, useEffect} from 'react';
import {FlatList,StyleSheet,TouchableOpacity,Alert,View, Text, Image, Dimensions} from 'react-native';

export default function CharactersDetailsScreen({navigation}) {
    return(
        <Text>Heloo</Text>
    );
}